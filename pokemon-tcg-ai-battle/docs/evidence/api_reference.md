# Support Platform API Reference Documentation

This documentation is reflectively generated by `api_docs.py` scanning the public API.

## Module `contracts`
Contracts and utility functions for Offline Training support.

Provides JSON validation, atomic file writes, canonical hashing, and locking.

### `FileLock` (class)
**Signature**: `FileLock(lock_path: 'str | Path', timeout: 'float' = 10.0, stale_threshold: 'float' = 30.0)`
PID/Hostname/Time based file locking mechanism.

### `FileLockError` (class)
Raised when file lock acquisition times out.

### `SupportContractError` (class)
Raised when an operation or data violates the support contract.

### `atomic_write_json` (function)
**Signature**: `atomic_write_json(path: 'str | Path', value: 'object') -> 'None'`
Atomically write value as canonical JSON to destination path.

### `atomic_write_records` (function)
**Signature**: `atomic_write_records(path: 'str | Path', records: 'Iterable[dict[str, object]]') -> 'int'`
Atomically write records as JSONL to destination path.

### `canonical_json` (function)
**Signature**: `canonical_json(value: 'object') -> 'str'`
Serialize value to a stable, sorted JSON string without non-finite values.

### `dict_raise_on_duplicates` (function)
**Signature**: `dict_raise_on_duplicates(ordered_pairs: 'list[tuple[str, Any]]') -> 'dict[str, Any]'`
Raise SupportContractError if duplicate keys are detected in JSON parsing.

### `digest` (function)
**Signature**: `digest(value: 'object', *, domain: 'str' = 'support') -> 'str'`
Generate SHA-256 hash of canonical JSON with domain salt.

### `is_pid_active` (function)
**Signature**: `is_pid_active(pid: 'int') -> 'bool'`
Check if a process with given PID is running on local system.

### `load_records` (function)
**Signature**: `load_records(path: 'str | Path') -> 'list[dict[str, Any]]'`
Load JSONL records from path safely.

### `safe_json_loads` (function)
**Signature**: `safe_json_loads(s: 'str') -> 'Any'`
Parse JSON string and reject duplicate keys.

### `walk_safe` (function)
**Signature**: `walk_safe(value: 'object', *, path: 'str' = '$') -> 'None'`
Ensure no private keys, paths, or non-finite values exist in the object.

## Module `statistics`
Evaluation statistics module.

Provides win rate metrics, Wilson score intervals, and stratified bootstrap.

### `compute_z_score` (function)
**Signature**: `compute_z_score(confidence: 'float') -> 'float'`
Approximate the z-score for a given two-sided confidence level.

### `evaluate_game_statistics` (function)
**Signature**: `evaluate_game_statistics(games: 'Iterable[dict[str, Any]]', draw_weight: 'float' = 0.5) -> 'dict[str, Any]'`
Aggregate per-game records to produce detailed evaluation metrics.

### `run_stratified_bootstrap` (function)
**Signature**: `run_stratified_bootstrap(games: 'list[dict[str, Any]]', draw_weight: 'float' = 0.5, num_samples: 'int' = 1000, confidence: 'float' = 0.95, seed: 'int' = 42) -> 'tuple[float, float]'`
Calculate bootstrap confidence interval stratified by candidate_seat.

### `wilson_score_interval` (function)
**Signature**: `wilson_score_interval(wins: 'float', losses: 'float', draws: 'float', draw_weight: 'float' = 0.5, confidence: 'float' = 0.95) -> 'tuple[float, float]'`
Calculate the Wilson score interval for the win rate.

## Module `ratings`
Ratings calculation module.

Provides Elo and Bradley-Terry rating models using Python standard library.

### `check_connected` (function)
**Signature**: `check_connected(policies: 'list[str]', matchups: 'dict[tuple[str, str], int]') -> 'bool'`
Detect if the policy match graph is fully connected.

### `compute_bradley_terry` (function)
**Signature**: `compute_bradley_terry(games: 'list[dict[str, Any]]', max_iter: 'int' = 1000, tol: 'float' = 1e-06, regularization: 'float' = 0.01) -> 'dict[str, Any]'`
Compute Bradley-Terry ratings using a regularized MM algorithm.

### `compute_elo` (function)
**Signature**: `compute_elo(games: 'list[dict[str, Any]]', initial_rating: 'float' = 1500.0, k_factor: 'float' = 32.0, draw_weight: 'float' = 0.5) -> 'dict[str, Any]'`
Calculate Elo ratings sequentially and deterministically.

## Module `registries`
Registry management module.

Implements dataset, model, experiment, deck, and opponent registries.
Uses local file-based storage with indices, logs, and atomic updates.

### `BaseRegistry` (class)
**Signature**: `BaseRegistry(dir_path: 'Path', schema_version: 'str', id_field: 'str')`
Base logic for a single entity type registry.

### `DatasetRegistry` (class)
**Signature**: `DatasetRegistry(root_dir: 'Path')`
Dataset lineage and verification registry.

### `DeckRegistry` (class)
**Signature**: `DeckRegistry(root_dir: 'Path')`
Legal deck list tracking registry.

### `ExperimentRegistry` (class)
**Signature**: `ExperimentRegistry(root_dir: 'Path')`
Offline/screening run performance registry.

### `ModelRegistry` (class)
**Signature**: `ModelRegistry(root_dir: 'Path')`
Model evaluation and benchmarking registry.

### `OpponentRegistry` (class)
**Signature**: `OpponentRegistry(root_dir: 'Path')`
Opponent configurations tracking registry.

### `SupportRegistryManager` (class)
**Signature**: `SupportRegistryManager(root_dir: 'str | Path')`
Consolidated registry manager coordinating all sub-registries.

## Module `errors`
Common error taxonomy for offline training support platform.

Provides secure exception classes with separated safe messages and private contexts.

### `ChecksumError` (class)
**Signature**: `ChecksumError(message: 'str', private_context: 'Optional[dict[str, Any]]' = None) -> 'None'`
Raised when checksum mismatch occurs.

### `CompatibilityError` (class)
**Signature**: `CompatibilityError(message: 'str', private_context: 'Optional[dict[str, Any]]' = None) -> 'None'`
Raised when components are incompatible.

### `ConcurrencyError` (class)
**Signature**: `ConcurrencyError(message: 'str', private_context: 'Optional[dict[str, Any]]' = None) -> 'None'`
Raised when concurrency collisions occur.

### `ConflictError` (class)
**Signature**: `ConflictError(message: 'str', private_context: 'Optional[dict[str, Any]]' = None) -> 'None'`
Raised when resource conflict happens.

### `CorruptionError` (class)
**Signature**: `CorruptionError(message: 'str', private_context: 'Optional[dict[str, Any]]' = None) -> 'None'`
Raised when registries or caches are corrupted.

### `InsufficientEvidenceError` (class)
**Signature**: `InsufficientEvidenceError(message: 'str', private_context: 'Optional[dict[str, Any]]' = None) -> 'None'`
Raised when insufficient evidence is provided for promotion.

### `IntegrityError` (class)
**Signature**: `IntegrityError(message: 'str', private_context: 'Optional[dict[str, Any]]' = None) -> 'None'`
Raised when data integrity issues are detected.

### `LockError` (class)
**Signature**: `LockError(message: 'str', private_context: 'Optional[dict[str, Any]]' = None) -> 'None'`
Raised when locking failures happen.

### `PrivacyError` (class)
**Signature**: `PrivacyError(message: 'str', private_context: 'Optional[dict[str, Any]]' = None) -> 'None'`
Raised when privacy violations are detected.

### `ResourceLimitError` (class)
**Signature**: `ResourceLimitError(message: 'str', private_context: 'Optional[dict[str, Any]]' = None) -> 'None'`
Raised when resource budget is exceeded.

### `SchemaError` (class)
**Signature**: `SchemaError(message: 'str', private_context: 'Optional[dict[str, Any]]' = None) -> 'None'`
Raised when schema validation fails.

### `SupportError` (class)
**Signature**: `SupportError(code: 'str', message: 'str', private_context: 'Optional[dict[str, Any]]' = None, severity: 'str' = 'ERROR', retryable: 'bool' = False) -> 'None'`
Base exception class for all support platform errors.

### `UnsafeOperationError` (class)
**Signature**: `UnsafeOperationError(message: 'str', private_context: 'Optional[dict[str, Any]]' = None) -> 'None'`
Raised when unsafe execution is blocked.

### `UnsupportedVersionError` (class)
**Signature**: `UnsupportedVersionError(message: 'str', private_context: 'Optional[dict[str, Any]]' = None) -> 'None'`
Raised when version is not supported.

### `ValidationError` (class)
**Signature**: `ValidationError(message: 'str', private_context: 'Optional[dict[str, Any]]' = None) -> 'None'`
Raised when validation fails.

## Module `json_schema`
JSON Schema equivalent definition generator for offline training platform.

Provides deterministic dictionary schema outputs without relying on external libraries.

### `get_schema` (function)
**Signature**: `get_schema(name: 'str') -> 'dict[str, Any]'`
Retrieve schema by key safely.

### `validate_dict_by_schema` (function)
**Signature**: `validate_dict_by_schema(data: 'dict[str, Any]', schema: 'dict[str, Any]') -> 'tuple[bool, str]'`
Perform a light, secure dictionary validation against a json schema dictionary.

### `validate_record` (function)
**Signature**: `validate_record(data: 'dict[str, Any]', schema_name: 'str') -> 'None'`
Validate dictionary against registered schema name, raising error if invalid.
