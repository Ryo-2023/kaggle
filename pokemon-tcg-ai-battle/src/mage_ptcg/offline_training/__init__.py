"""Offline Training v1 — resumable neural Student training pipeline.

This package adds a unified, resumable pipeline on top of the existing C4
collection engine, Stable ActionKey feature extraction, and Rule Agent v0
fallback.  It never modifies the linear Student baseline, the default Champion
agent, or the existing C4 submission package.
"""

from __future__ import annotations

__all__: list[str] = []
